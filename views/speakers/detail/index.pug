extends ../../layout/index
block home
    .container#speakerDetail
     .row
        .col-sm-8
            article.speakerslist
                .container
                .row
                    .col-sm-8
                    h3.speakerslist-title=speakerTitle
                    h5.speakerslist-name
                        | with 
                        a(href=`/speakres/${speakerShortname}`)=speakerName
                    p.speakerslist-info
                        a(href=`/${speakerShortname}`)
                        img.speakerslist-img.img-responsive.pull-left(src=`/images/speakers/${speakerShortname}.jpg`, alt=`Photo of ${speakerName}`)
                    !=speakerSummary
            // row 
            // container 
        // primary 
        aside.col-sm-4
         article.sidebar
            include ../../includes/artwork